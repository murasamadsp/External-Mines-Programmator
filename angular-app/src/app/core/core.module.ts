import { NgModule } from "@angular/core";

@NgModule({
  // No declarations needed; services are providedIn: 'root'
  // This module can be imported in the root component to group core services.
})
export class CoreModule {}
