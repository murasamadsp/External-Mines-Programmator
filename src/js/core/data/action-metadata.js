/**
 * Action UI Metadata
 * Maps action names to labels and tooltips
 * Використовує повний набір метаданих для всіх дій
 */

import { ACTION_METADATA_COMPLETE } from "./action-metadata-complete.js";
import { ACTION_CATEGORIES } from "./action-categories.js";

// Використовуємо повний набір метаданих
export const ACTION_METADATA = ACTION_METADATA_COMPLETE;

export { ACTION_CATEGORIES };
