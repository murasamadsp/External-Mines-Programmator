# External Mines Programmator

Веб-приложение для визуального программирования ботов в игре Mines. Поддерживает только Base64 формат для максимальной эффективности и простоты.

## 🚀 Функции

- **Визуальное программирование**: Интуитивный интерфейс с сеткой 16x12 для размещения команд
- **Base64 сериализация**: Эффективное сжатие программ с использованием deflate
- **Валидация**: Проверка корректности программ с подсказками об ошибках
- **Импорт/Экспорт**: Быстрая загрузка и выгрузка программ в Base64 формате
- **Модульная архитектура**: Чистый, поддерживаемый код на ES6 модулях

## 🏗️ Архитектура

Проект полностью переписан на модульной архитектуре:

```
src/js/modules/
├── constants.js    # Константы, enum-ы и конфигурация
├── program.js      # Класс Program для управления инструкциями
├── serializer.js   # Base64 сериализация/десериализация
├── ui.js          # Пользовательский интерфейс
└── main.js        # Инициализация приложения
```

## 🎯 Использование

1. **Запуск**: `npm run dev`
2. **Выберите действие** из палитры команд
3. **Кликните на сетку** для размещения команд
4. **Экспорт**:
   - "Export to Codes" - числовые коды через пробел (для шахты) ⭐
   - "Export to Text (v3)" - текстовый формат (для просмотра/редактирования)
   - "Export to Base64" - бинарный формат (для сохранения)
5. **Импорт**: Вставьте строку любого поддерживаемого формата - программа загрузится в сетку
6. **Валидация**: Проверьте программу кнопкой "Validate Program"

## 📋 Поддерживаемые форматы

### Base64 v3 (компактный)
- **Экспорт**: Только заполненные инструкции (компактный формат)
- **Метки**: "0" для всех инструкций без специальных меток
- **Использование**: Для сохранения и обмена программами

### Text v3 (символьный)
- **Экспорт**: Только заполненные инструкции
- **Формат**: `^W` (вверх), `^A` (влево), `^S` (вниз), `^D` (вправо) и т.д.
- **Сжатие**: Пробелы заменяются на `.` и `_`

### Codes (числовые коды)
- **Экспорт**: Только заполненные инструкции
- **Формат**: Числовые коды через пробел (4 4 4 4)
- **Для шахты**: Совместим с Python скриптом decypher.py

### Примеры:
- **Base64**: `BAAAAAQEBAQwOjA6MDow` (4 стрелки вверх)
- **Text**: `$^W^W^W^W` (4 стрелки вверх)
- **Codes**: `4 4 4 4` (4 стрелки вверх)

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run serve
```

## 🔧 Технологии

- **Vite**: Быстрая сборка и dev-сервер
- **ES6 Modules**: Модульная архитектура
- **Pako**: Deflate сжатие для Base64
- **CSS Grid**: Адаптивная сетка интерфейса
- **HTML5**: Семантическая разметка

## 📋 Поддерживаемые команды

Программатор поддерживает все команды Mines, включая:
- Движение и поворот
- Строительство и копание
- Условные операторы
- Переходы и вызовы
- Переменные и сравнения
- Системные команды

## 🎨 Кастомизация

Стили находятся в `src/assets/styles/`. Основные компоненты:
- `.programmer-container` - главный контейнер
- `.action-palette` - палитра команд
- `.program-grid` - сетка программы
- `.program-controls` - панель управления

## 🔐 Безопасность

- Клиентская обработка данных
- Нет серверной части
- Локальное хранение программ
- Валидация входных данных

## 📄 Лицензия

ISC License - см. package.json для деталей.

## 🤝 Вклад

Приветствуются улучшения интерфейса, оптимизации производительности и новые функции!