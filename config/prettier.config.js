module.exports = {
  // Code Guide compliant settings
  semi: true, // Always use semicolons
  trailingComma: 'es5', // Trailing commas for ES5
  singleQuote: true, // Single quotes for strings
  printWidth: 80, // Max line length
  tabWidth: 2, // 2 spaces for indentation
  useTabs: false, // Use spaces, not tabs
  bracketSpacing: true, // Space after brackets
  arrowParens: 'avoid', // Omit parens when possible
  endOfLine: 'lf', // Unix line endings
  quoteProps: 'as-needed', // Quote only when needed
  jsxSingleQuote: true, // Single quotes in JSX
  bracketSameLine: false, // Put > on new line in JSX
};
